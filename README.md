# lockd

Dart CLI for code generation. Run from the project directory:

```sh
dart run lockd
```

This prints `locked`. Entrypoint is `bin/lockd.dart`; shared code lives under `lib/`; tests under `test/`.
